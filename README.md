# AI POC - 건강기능식품 OEM 견적 챗봇

저비용 LLM + 검색 API + 원료 DB를 활용한 건강기능식품 OEM 자동 견적 시스템

## 주요 기능

### 1. OEM 자동 견적
- **빠른 견적**: 폼 입력으로 즉시 견적 계산
- **대화형 견적**: 자연어로 견적 요청 (LLM 기반)
- **레퍼런스 제품**: 시중 제품 배합비 참조
- **MOQ 자동 적용**: 최소주문수량 규칙 자동 반영

### 2. 검색 챗봇
- 4개 검색 API × 4개 LLM = **16가지 조합** 테스트
- Fallback 로직: 실패 시 자동으로 다른 API 시도

## 기술 스택

### LLM API (4개)
| 모델 | Input/Output ($/1M) | 용도 |
|------|---------------------|------|
| Gemini 2.0 Flash | $0.10 / $0.40 | 기본 |
| GPT-5-nano | $0.05 / $0.40 | 최저가 |
| GPT-5-mini | $0.25 / $2.00 | 품질 비교 |
| GPT-4o-mini | $0.15 / $0.60 | 안정성 비교 |

### 검색 API (4개)
| API | 무료 한도 | 용도 |
|-----|----------|------|
| DDGS | 무제한* | 레퍼런스 제품 검색 |
| Brave | 2,000/월 | Fallback |
| Tavily | 1,000/월 | AI 특화 검색 |
| Google CSE | 100/일 | 정확한 검색 |

### 원료 DB
| 데이터 | 내용 |
|--------|------|
| 원료 가격 | 주원료, 부원료, 부형제 |
| 임가공비 | 제환비, 분말충진비, 정제비, 과립비 |
| 포장비 | 스틱포장, 단박스, 병포장 |
| MOQ 규칙 | 최소 2,000박스 또는 300kg |
| 레퍼런스 | 시중 제품 배합비 |

## 설치

```bash
# 가상환경 생성 (권장)
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 의존성 설치
pip install -r requirements.txt

# 환경변수 설정
cp .env.example .env
# .env 파일에 API 키 입력
```

## 실행

```bash
streamlit run app/main.py
```

브라우저에서 http://localhost:8501 접속

## 프로젝트 구조

```
99_ai_POC/
├── app/
│   ├── main.py                 # Streamlit 메인 앱
│   ├── config.py               # 설정
│   ├── providers/
│   │   ├── search.py           # 검색 API (4개)
│   │   └── llm.py              # LLM API (4개)
│   ├── services/
│   │   ├── ingredient.py       # 원료 DB 조회
│   │   ├── calculator.py       # 견적 계산
│   │   └── reference.py        # 레퍼런스 제품 검색
│   └── utils/
│       └── prompt.py           # 프롬프트 템플릿
├── data/
│   └── ingredient_db.json      # 원료/임가공/MOQ 데이터
├── docs/
│   ├── API_KEY_SETUP_GUIDE.md
│   └── POC_PLAN_V2.md
├── .env.example
├── requirements.txt
└── README.md
```

## 사용 예시

### 빠른 견적
1. 사이드바에서 "OEM 견적" 모드 선택
2. "빠른 견적" 탭에서:
   - 주원료 선택 (예: 차전자피)
   - 제형 선택 (환/분말/정제/과립)
   - 규격 입력 (g/포, 포/박스, 박스수)
   - 주원료 비율 설정
3. "견적 계산" 클릭

### 대화형 견적
```
사용자: 차전자피 환을 만들고 싶어. 5g씩 30포가 1박스고, 총 3000박스 제작할 예정이야

시스템: [제품 정보 파싱 + 레퍼런스 제품 제안]
        비율을 잘 모르시면 레퍼런스 제품의 비율을 적용할까요?

사용자: 차전자피 80%, 나머지는 알아서 해줘

시스템: [견적 계산 결과]
        💰 총 예상금액: 12,670,500원 (VAT 별도)
        박스당 단가: 4,223원
```

## 견적 계산 로직

```python
# 1. 총 원료량
total_kg = gram_per_pouch × pouch_per_box × boxes / 1000

# 2. 원료비
ingredient_cost = Σ(total_kg × ratio × price_per_kg)

# 3. 포장비
packaging_cost = (pouches × 스틱포장비) + (boxes × 단박스비)

# 4. 임가공비
processing_cost = total_kg × 제환비(또는 분말충진비 등)

# 5. 총 견적
total = ingredient_cost + packaging_cost + processing_cost
```

## API 키 설정

```bash
# .env 필수 항목
OPENAI_API_KEY=sk-...        # GPT 모델용
GOOGLE_API_KEY=AIza...       # Gemini용

# 선택 항목 (검색 API)
BRAVE_API_KEY=BSA...         # Brave Search
TAVILY_API_KEY=tvly-...      # Tavily
GOOGLE_CSE_API_KEY=AIza...   # Google CSE
GOOGLE_CSE_ID=...            # Google CSE
```

자세한 발급 방법은 [API_KEY_SETUP_GUIDE.md](docs/API_KEY_SETUP_GUIDE.md) 참고

## 예상 비용 (일 100건 견적 기준)

| LLM | 월 비용 |
|-----|--------|
| GPT-5-nano | ~$0.50 |
| Gemini 2.0 Flash | ~$0.50 |
| GPT-4o-mini | ~$0.80 |
| GPT-5-mini | ~$2.20 |

> 검색 API: 무료 티어 조합으로 **$0**

## License

MIT
